const PIC_MIN_WAIT_LOADED_DURATION = Duration(milliseconds: 100); //the minimum time a pic has to wait to be shown even if it is already loaded
const PIC_SLIDE_IN_ANIMATION_DURATION = Duration(milliseconds: 1000); //the time the sliding animation takes to show/hide the pics
const OPACITY_ANIMATION_DURATION = Duration(milliseconds: 500);
